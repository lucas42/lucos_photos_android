<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for photo and video backup -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!--
        POST_NOTIFICATIONS is required on API 33+ to post system notifications.
        Used to alert the user when a newer version of the app is available.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        WAKE_LOCK allows WorkManager to keep the device awake while a background sync is running.
        WorkManager's own AAR includes this in its manifest merge, but declaring it explicitly
        here is the safe and recommended approach to ensure syncs aren't interrupted mid-way
        when the screen turns off.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".PhotoBackupApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Disable WorkManager's default androidx.startup-based initialisation so that
            PhotoBackupApplication can initialise WorkManager manually with a custom
            WorkerFactory. Without this, WorkManager initialises itself before our
            Application.onCreate() runs and ignores the factory we'd like to register.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
